
hello {
    JPS copy_to_tilemap
    JPS write_to_palette_0 // test out comments
    /*
    more comments
    */
    JPS write_to_framebuffer "hi"
    LOAD A, $000
    SET A, [A]
    SET B, 'g'; ADD A, B; // yes, this works
    AND B, 0sMMM
    SET C, B
    OR C, 0t---000++0
    "Some text that will assemble!" "this is \" a string too"
    JP *, $000, [D]; JPN copy_to_tilemap; JP *, *, *;
    // macro test
    CALL func x, y
    // should assemble to PUSH B; PUSH C; SET B, x; SET C, y; JPS func; POP C; POP B;
}

data
{
    0sAAA; 0sBBB; 0sCCC;
}

test
{
    "Hello!"
}